# 复制为 .env.local 后修改（Next.js 只读取 frontend/ 根目录下的 .env*）
#
# 本地前端 + 远端测试 API：
NEXT_PUBLIC_API_BASE_URL=https://coding-agent.zood.work
#
# 本地前端 + 本地后端（默认，可不配置）：
# NEXT_PUBLIC_API_BASE_URL=http://localhost:7001
